{
  "name": "voicemaster-app",
  "version": "1.0.0",
  "description": "«·„œ—» «·’Ê Ì -  ÿ»Ìﬁ «·Â« ›",
  "private": true,
  "scripts": {
    "sync": "npx cap sync",
    "open:android": "npx cap open android",
    "build:android": "npx cap sync && npx cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/android": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
